<%= form_for(@schedules_event) do |f| %>
  <% if @schedules_event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@schedules_event.errors.count, "error") %> prohibited this schedules_event from being saved:</h2>

      <ul>
      <% @schedules_event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :schedule_id %><br />
    <%= f.number_field :schedule_id %>
  </div>
  <div class="field">
    <%= f.label :event_id %><br />
    <%= f.number_field :event_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
