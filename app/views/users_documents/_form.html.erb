<%= form_for(@users_document) do |f| %>
  <% if @users_document.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@users_document.errors.count, "error") %> prohibited this users_document from being saved:</h2>

      <ul>
      <% @users_document.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
